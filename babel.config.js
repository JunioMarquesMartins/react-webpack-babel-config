module.exports = {
    presets: [
        '@babel/preset-env', // convertir código actual a código compatible con otros entornos 
        '@babel/preset-react' // añadir funcionalidades del react en la conversión
    ]
};